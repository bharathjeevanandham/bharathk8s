apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-prometheus
  namespace: prometheus
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/prometheus
  storageClassName: ""
